import React from 'react';
import {Container, Header, Content, Text} from 'native-base';

const AddDeck = () => (
	<Container>
		<Header/>
		<Content>
			<Text>AddDeck</Text>
		</Content>
	</Container>
);

export default AddDeck;
